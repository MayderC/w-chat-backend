export default interface IEnvronment {
  JWT_KEYWORD: string;
  DB_STRING_CONECTION: string;
  DB_USER: string;
  DB_PASSWORD: string;
  DB_DIALECT: string;
  DB_NAME: string;
  DB_HOST: string;
  PORT: string;
  NODE_ENV: string;
  CLIENT_URL: string;
}
